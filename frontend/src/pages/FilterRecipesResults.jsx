// import Card from "../components/card/card";
// import {useDate} from "../services/Context"

function FilterRecipeResults() {
  // const data = useData()

  return (
    <div>
      {/* {data.map((card) => {
        return (
          <Card
            key={data.hits[index].recipe.label}
            data={data.hits[index].recipe}
          />
        );
      })} */}

      <h2>Bonjour filter</h2>
    </div>
  );
}

export default FilterRecipeResults;
